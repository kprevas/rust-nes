language: rust
cache: cargo
dist: trusty
os:
  - linux
  - osx
rust:
  - stable
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y gtk+-3.0 glib-2.0
script:
  - cargo build
  - cargo test